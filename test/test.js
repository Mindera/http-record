/*global describe, it */
'use strict';
var assert = require('assert');
var httpRecord = require('../');

describe('http-record node module', function () {
  it('must have at least one test', function () {
    httpRecord();
    assert(false, 'TODO: I need to write some tests for this!');
  });
});
